spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8787
  servlet:
    context-path: /

logging:
  level:
    root: info
    com.example.xlsprocessor: debug